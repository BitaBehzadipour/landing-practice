'use strict';
const nav=document.querySelector('nav');
const menulink=document.querySelector('menu-link');
nav.addEventListener('mouseenter',function(e){
    const link=e.target;
    const sibling=link.closest('nav').querySelector('menu-link');
    if(link!==)
})